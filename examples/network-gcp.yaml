apiVersion: multik8s.platformref.crossplane.io/v1alpha1
kind: Network
metadata:
  name: network-gcp
spec:
  id: multik8s-network
  clusterRef:
    id: multik8s-cluster
  compositionSelector:
    matchLabels:
      provider: GCP
